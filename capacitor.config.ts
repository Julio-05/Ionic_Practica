import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tarea_3_ionic_app_mobile',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
